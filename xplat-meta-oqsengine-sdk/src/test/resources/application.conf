akka.grpc.client {
  "CheckService" {
    service-discovery {
      service-name = "CheckService"
    }
//        host = localhost
//        port = 8081
//        host = "localhost"
    host = "120.55.249.44"
    host = ${?BOCP_SERVER_HOST}
    port = 23120
//        port = 8081
    port = ${?BOCP_SERVER_PORT}
    override-authority = "google.fr"
    deadline = infinite
    user-agent = "Akka-gRPC"
    use-tls = false
    use-tls = ${?USE_TLS}
  }

  "NodeService" {
    service-discovery {
      service-name = "NodeService"
    }
//    host = localhost
//    port = 8081
    //    host = "localhost"
    host = "120.55.249.44"
    host = ${?BOCP_SERVER_HOST}
    port = 23120
    //    port = 8081
    port = ${?BOCP_SERVER_PORT}
    override-authority = "google.fr"
    deadline = infinite
    user-agent = "Akka-gRPC"
    use-tls = false
    use-tls = ${?USE_TLS}
  }

  "EntityService" {
    service-discovery {
      service-name = "EntityService"
    }
//
//    host = "localhost"
    host = "120.55.249.44"
    host = ${?OSQENGINE_SERVER_HOST}
//        port = 8082
    port = 23118
    port = ${?OSQENGINE_SERVER_PORT}
    override-authority = "google.fr"
    deadline = infinite
    user-agent = "Akka-gRPC"
    use-tls = false
    use-tls = ${?USE_TLS}
  }

  "DictCheckService" {
    service-discovery {
      service-name = "DictCheckService"
    }
//        host = "localhost"
    //    host = "localhost"
    host = "120.55.249.44"
//        port = 8081
    host = ${?BOCP_SERVER_HOST}
    port = 23120
    //    port = 8081
    port = ${?BOCP_SERVER_PORT}
    override-authority = "google.fr"
    deadline = infinite
    user-agent = "Akka-gRPC"
    use-tls = false
    use-tls = ${?USE_TLS}
  }
}

akka {
  loglevel = "DEBUG"
}


pfcp {
  accessUri = "http://pfcp.phoenix-t.xforceplus.com"
  accessUri = ${?PFCP_ACCESSURI}
}

